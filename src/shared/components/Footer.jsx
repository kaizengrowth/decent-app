import React from 'react';

import { NAME } from '../config';

const Footer = () => (
  <div className="container mt-5">
    <hr />
    <footer>
      <p>{ NAME }</p>
    </footer>
  </div>
);

export default Footer;
